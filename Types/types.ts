/**
 * Här skriver vi alla våra typer nödvändiga för vårt projekt.
 */

// ÄNDRA SÅ ATT DET BARA FINNS EN AV DESSA
export type old_player = {
    username: string;
    password: string;
    elo: number;
};

export type Player = {
    name: string;
    Pass: string;
    elo: number;
};